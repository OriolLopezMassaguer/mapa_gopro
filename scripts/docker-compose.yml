services:
  app:
    build: .
    ports:
      - "${PORT:-3001}:3001"
    volumes:
      - "${DATA_DIR}:/data:ro"
    environment:
      DATA_DIR: /data
      PORT: 3001
      CACHE_CONCURRENCY: "${CACHE_CONCURRENCY:-4}"
    restart: unless-stopped
